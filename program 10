//Calculate area of a triangle by taking values of edges from user
//h=height,b=base


#include<stdio.h>
int main(){

    float h,b,area;

printf("Enter the values of h and b : \n");
scanf("%f %f",&h,&b);

area= 0.5*h*b;

printf("The area of the triangle is : %f\n",area);

return 0;
}
